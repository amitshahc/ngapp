import { InputLowercaseDirective } from './input-lowercase.directive';

describe('InputLowercaseDirective', () => {
  it('should create an instance', () => {
    const directive = new InputLowercaseDirective();
    expect(directive).toBeTruthy();
  });
});
